import { typedMemo } from "core/utils/typedMemo";
import { FC } from "react";

export const RestorePasswordPage: FC<Props> = typedMemo(function RestorePasswordPage(props){
    return (
        
    )
})